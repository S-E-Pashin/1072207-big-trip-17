// import {FilterType} from '../const';
// import {isTaskExpired, isTaskExpiringToday, isTaskRepeating} from './task';
//
// const filter = {
//   [FilterType.EVERYTHING]: (tasks) => tasks.filter((task) => !task.isArchive),
//   [FilterType.FUTURE]: (tasks) => tasks.filter((task) => isTaskExpired(task.dueDate) && !task.isArchive),
//   [FilterType.PAST]: (tasks) => tasks.filter((task) => isTaskExpiringToday(task.dueDate) && !task.isArchive),
// };
//
// export {filter};
